---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - servicex-repository.yaml
  - servicex-release.yaml
  - servicex-int-1-sealed-secrets.yaml
  - values.yaml

secretGenerator:
  - name: servicex-secrets
    namespace: servicex-int-1
    type: Opaque

configurations:
  - kustomizeconfig.yaml
